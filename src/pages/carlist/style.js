import styled from 'styled-components'

export const ContainerCarList = styled.div`
    display: flex;
    align-items: center;
    justify-content: center;
    flex-wrap: wrap;
    gap: 2rem;

    width: 90%;
    margin: 3rem auto;
   
`

export const SwitchPage = styled.select`
    margin: 1rem;
    outline: none;
    border: none;

    padding: 1rem;
    font-size: 1rem;

    box-shadow: 0px 4px 4px rgba(0, 0, 0, 0.25);
`